/**
 * Linux high performance network driver for DataFlo
 * 
 * This driver tries to be as high performance as possible. But it may be a PITA to use.
 * Likely will require OS configuration or privilege escalation.
 * 
 * Only use this when there are throughput or latency requirements.
 */

namespace df
{
}
